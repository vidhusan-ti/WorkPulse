# Daily Notes — 2026-06-30

## Session: WorkPulse onboarding flow

- Vidhu asked about OS-independent download commands for WorkPulse
- Clarified: pip install from GitHub is the recommended method
- Added `_first_run_setup()` wizard to `src/cli_monitor.py`:
  - Prompts for ANTHROPIC_API_KEY on first run
  - Prompts for Cursor transcript path if not auto-detected
  - Saves both, then starts automatically
  - Subsequent runs skip the wizard entirely
- Vidhu asked how to reopen after first run → `workpulse-monitor` + browser at localhost:7700
- Vidhu requested persistent cross-session memory → set up MEMORY.md + memory/ directory

## Key decisions

- API key saved to `~/.workpulse/.env` (not config.json, not git)
- Transcript path saved to `~/.workpulse/config.json`
