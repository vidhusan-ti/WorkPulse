# Arleif — Above Bar Prompts (v1)

## Window 1

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/07653483-26ce-4cc7-8b23-0af4d92789a2/07653483-26ce-4cc7-8b23-0af4d92789a2.jsonl`

**Prompt:**
> are you sure about this? "If deleting domain A breaks domain B at compile time, A and B are coupled through something other than a published contract" our philosophy is compile time failure over runtime, that means we will probably have sdk style calls not loose http requests that are not compile time checked

**Why above bar:** Arleif catches a contradiction between the domain isolation principle as stated by the LLM and the team's own stated philosophy — compile-time safety — and uses that contradiction to sharpen what "coupling" means in their context. The correction is architecturally substantive and redirects the design spec in a direction the LLM had not taken.

---

## Window 2

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/07653483-26ce-4cc7-8b23-0af4d92789a2/07653483-26ce-4cc7-8b23-0af4d92789a2.jsonl`

**Prompt:**
> I don't mind it being stack specific. Our stack will be supabase as DB, angular as frontend, analogjs, apis in analogjs, NO supabase edge functions. I think it is better to have guidance around the stack than leaving that decision making open and variable every time it runs. do we have guidance about how to resolve cross domain concerns and the need for joins etc? When to use db functions and when not to (I want to keep their use to a bare minimum so as not to split business logic between app and db)

**Why above bar:** Arleif contributes a clear architectural constraint (no edge functions, no split business logic between app and db) that the LLM had not established, and names the exact risk (logic fragmentation across layers). The outcome is a concretely scoped domain architecture prompt rather than a generic one.

---

## Window 3

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/07653483-26ce-4cc7-8b23-0af4d92789a2/07653483-26ce-4cc7-8b23-0af4d92789a2.jsonl`

**Prompt:**
> I want to simplify this further. I don't want us polluting the design folder with a lot of content that gets outdated, like flows. Let us keep it to ADRs, and domain-map.md. So I'm suggesting that the architects produce domain-map and a single design doc in .rebaseai/... just like it was done before (treated as temporary disposable). The coder can work off these two documents.

**Why above bar:** Arleif identifies the risk of design-folder entropy — a production maintenance concern the LLM had not raised — and makes a concrete simplification decision (ADRs + domain-map only, no flows) with a clear rationale. This shapes the artifact contract for the entire coder workflow.

---

## Window 4

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/608687b7-01ff-47ad-83eb-8310eab206dd/608687b7-01ff-47ad-83eb-8310eab206dd.jsonl`

**Prompt:**
> It is a nightmare making this team of agents the concierge has work. The routing is unreliable, keeping the state in order is just too hard. We did this to get more predictability, but that is not happening. I was concerned that 1 agent knowing all the different workflows would be too much to expect it to get right. But this is no better. The routing is probably our biggest problem...

**Why above bar:** Arleif diagnoses a systemic failure (routing unreliability, state management complexity) at the right level of abstraction and frames the trade-off honestly — they had optimised for predictability but the architecture was not delivering it. This reframing led to a concrete redesign of the concierge architecture; the user drove both the diagnosis and the direction.

---

## Window 5

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/85f482cd-872a-4f9e-9ac8-ef1eda014fb5/85f482cd-872a-4f9e-9ac8-ef1eda014fb5.jsonl`

**Prompt:**
> i think the right thing to do is kill chaining or kill dynamic system prompts and not fight this. There are other places where more stuff than state is changed in the prompt, our prompts are broken into many parts and then merged. I think short term we'll switch to non-OpenAI, maybe Anthropic, and deal with this later. I prefer not doing dynamic prompts

**Why above bar:** After the LLM explained the chaining/dynamic-prompt conflict, Arleif synthesises the root cause, rejects the workaround options presented, and makes a decisive architectural call (kill dynamic prompts, switch provider). The insight is the trade-off awareness: short-term provider switch to unblock progress while deferring the deeper refactor — a judgment the LLM did not volunteer.

---

## Window 6

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/82a3118c-7689-4255-b2ad-2c638a8e88a0/82a3118c-7689-4255-b2ad-2c638a8e88a0.jsonl`

**Prompt:**
> I want to enhance my project-board-poll workflow to trigger coding and design without using LLMs. The Project team leader currently is very unreliable and makes a mess of the job it has, fetch its prompt from langfuse using token in .env and suggest how I can use deterministic workflows instead. I'm open to asking the human to put a label `rebaseai-design-approved` so it is easy for the workflow to identify and trigger these.

**Why above bar:** Arleif identifies the correct root cause (LLM-based orchestration is unreliable for deterministic label-driven routing) and proposes a concrete architectural shift — human labels as deterministic triggers — that removes a class of failure entirely. The design insight (label as machine-readable contract between human and workflow) is the user's own.
