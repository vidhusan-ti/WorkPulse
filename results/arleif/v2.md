# Arleif — Above Bar Prompts (v2)

## Window 1

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/07653483-26ce-4cc7-8b23-0af4d92789a2/07653483-26ce-4cc7-8b23-0af4d92789a2.jsonl`

**Prompt:**
> are you sure about this? "If deleting domain A breaks domain B at compile time, A and B are coupled through something other than a published contract" our philosophy is compile time failure over runtime, that means we will probably have sdk style calls not loose http requests that are not compile time checked

**Ownership test:** [new constraint ✅] [user-initiated ✅] [LLM extended ✅] [removal test ✅]

**Counterfactual:** Without this prompt, the LLM would have left the incorrect coupling definition in the architecture document — it accepted the wrong framing and had moved on without self-correcting.

**Why above bar:** Arleif identifies a direct contradiction between the LLM's stated principle and the team's documented philosophy, and supplies the corrective reasoning ("compile-time failure is the safety property working, not a defect"). The LLM accepted the correction without pushback and rewrote the spec accordingly.

---

## Window 2

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/07653483-26ce-4cc7-8b23-0af4d92789a2/07653483-26ce-4cc7-8b23-0af4d92789a2.jsonl`

**Prompt:**
> I don't mind it being stack specific. Our stack will be supabase as DB, angular as frontend, analogjs, apis in analogjs, NO supabase edge functions. I think it is better to have guidance around the stack than leaving that decision making open and variable every time it runs. do we have guidance about how to resolve cross domain concerns and the need for joins etc? When to use db functions and when not to (I want to keep their use to a bare minimum so as not to split business logic between app and db)

**Ownership test:** [new constraint ✅] [user-initiated ✅] [LLM extended ✅] [removal test ✅]

**Counterfactual:** Without this prompt, the LLM would have kept the architecture prompt stack-neutral and variable — the specific constraint (no edge functions, no split business logic between app and DB) would never have been baked in.

**Why above bar:** Note on decision-query gate: the turn includes a question about cross-domain guidance, but the binding constraints (no edge functions, business logic stays in app layer only) precede and dominate the question. Those constraints materially change the correct architecture output — they are not derivable from prior context. The question about DB functions is near_bar, but the constraint contribution passes all 4 ownership checks.

---

## Window 3

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/07653483-26ce-4cc7-8b23-0af4d92789a2/07653483-26ce-4cc7-8b23-0af4d92789a2.jsonl`

**Prompt:**
> I want to simplify this further. I don't want us polluting the design folder with a lot of content that gets outdated, like flows. Let us keep it to ADRs, and domain-map.md. So I'm suggesting that the architects produce domain-map and a single design doc in .rebaseai/... just like it was done before (treated as temporary disposable). The coder can work off these two documents.

**Ownership test:** [new constraint ✅] [user-initiated ✅] [LLM extended ✅] [removal test ✅]

**Counterfactual:** Without this prompt, the LLM would have continued adding artifacts to the design folder (it had just finished a multi-turn session adding more structured design content) — the entropy concern and the simplification to ADRs-only were not on the LLM's radar.

**Why above bar:** Arleif identifies a maintenance risk the LLM had not raised (design-folder entropy, content getting outdated like flows) and makes a concrete structural decision that defines the artifact contract for the entire coder workflow: two documents, one durable, one disposable.

---

## ~~Window 4 (REMOVED from v2)~~

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/608687b7-01ff-47ad-83eb-8310eab206dd/608687b7-01ff-47ad-83eb-8310eab206dd.jsonl`

**Prompt (v1):**
> It is a nightmare making this team of agents the concierge has work. The routing is unreliable, keeping the state in order is just too hard... What do you recommend? I'm open to changing the plan.

**Why removed under v2:** Fails the **decision-query gate**. Arleif describes the problem accurately, but the concluding ask ("What do you recommend?") delegates the insight generation entirely to the LLM. The LLM produces the architectural redesign (stop using a silent router, make concierge the responder). The user's contribution is a detailed problem statement — which is valuable, but is near_bar, not above_bar under v2.

---

## Window 4

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/85f482cd-872a-4f9e-9ac8-ef1eda014fb5/85f482cd-872a-4f9e-9ac8-ef1eda014fb5.jsonl`

**Prompt:**
> i think the right thing to do is kill chaining or kill dynamic system prompts and not fight this. There are other places where more stuff than state is changed in the prompt, our prompts are broken into many parts and then merged. I think short term we'll switch to non-OpenAI, maybe Anthropic, and deal with this later. I prefer not doing dynamic prompts

**Ownership test:** [new constraint ✅] [user-initiated ✅] [LLM extended ✅] [removal test ✅]

**Counterfactual:** Without this prompt, the LLM had presented technical workarounds for the chaining/dynamic-prompt conflict — it would have continued down that path; the decision to reject all workarounds and switch providers was the user's own.

**Why above bar:** After the LLM explained the conflict, Arleif synthesises the root cause, rejects the presented workaround options, and makes a decisive architectural call: eliminate dynamic prompts, switch to Anthropic short-term to unblock progress while deferring the deeper refactor. The trade-off judgment — net-negative complexity vs. clean break — is the user's contribution, not the LLM's.

---

## Window 5

**Source:** `transcrpts/arleif/.cursor/projects/workspaces-agno/agent-transcripts/82a3118c-7689-4255-b2ad-2c638a8e88a0/82a3118c-7689-4255-b2ad-2c638a8e88a0.jsonl`

**Prompt:**
> I want to enhance my project-board-poll workflow to trigger coding and design without using LLMs. The Project team leader currently is very unreliable and makes a mess of the job it has, fetch its prompt from langfuse using token in .env and suggest how I can use deterministic workflows instead. I'm open to asking the human to put a label `rebaseai-design-approved` so it is easy for the workflow to identify and trigger these. Don't start coding, just propose a design

**Ownership test:** [new constraint ✅] [user-initiated ✅] [LLM extended ✅] [removal test ✅]

**Counterfactual:** Without this prompt, the LLM was running and maintaining an LLM-based orchestration workflow — the architectural insight (replace LLM routing with human-applied label as deterministic trigger) is the user's own, and the LLM had not proposed it.

**Why above bar:** Arleif correctly diagnoses that the failure class (unreliable LLM-led routing) is structural, not tunable, and proposes a concrete architectural shift — human labels as machine-readable deterministic triggers — that removes the failure mode entirely. The design insight (label as contract between human intent and workflow automation) is the user's own.
